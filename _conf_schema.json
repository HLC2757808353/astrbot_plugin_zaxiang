{
  "cold_violence": {
    "description": "冷暴力功能配置",
    "type": "object",
    "hint": "AI冷暴力功能，防止被骚扰",
    "items": {
      "enabled": {
        "description": "是否启用冷暴力功能",
        "type": "bool",
        "default": true,
        "hint": "启用后AI可以冷暴力骚扰者"
      },
      "authority_ids": {
        "description": "权限ID列表",
        "type": "list",
        "default": [],
        "hint": "在此列表中的用户可以执行冷暴力和解除冷暴力指令"
      },
      "whitelist_ids": {
        "description": "白名单ID列表",
        "type": "list",
        "default": [],
        "hint": "在此列表中的用户不能被冷暴力"
      },
      "default_duration": {
        "description": "默认冷暴力时长（分钟）",
        "type": "int",
        "default": 30,
        "hint": "默认的冷暴力持续时间"
      }
    }
  },
  "mute_tracker": {
    "description": "禁言追踪配置",
    "type": "object",
    "hint": "追踪AI被禁言的情况，解禁后告知被谁禁言的",
    "items": {
      "enabled": {
        "description": "是否启用禁言追踪",
        "type": "bool",
        "default": true,
        "hint": "启用后AI被禁言解禁时会告知是谁禁言的"
      }
    }
  },
  "poke_reaction": {
    "description": "戳一戳反应配置",
    "type": "object",
    "hint": "AI被戳时有概率反戳，别人互戳时AI也有概率跟戳",
    "items": {
      "enabled": {
        "description": "是否启用戳一戳反应",
        "type": "bool",
        "default": true,
        "hint": "总开关，关闭后不响应任何戳一戳"
      },
      "back_probability": {
        "description": "反戳概率",
        "type": "float",
        "default": 0.5,
        "hint": "被戳时反戳回去的概率，0~1，默认0.5"
      },
      "follow_probability": {
        "description": "跟戳概率",
        "type": "float",
        "default": 0.3,
        "hint": "别人互戳时AI跟戳同一人的概率，0~1，默认0.3"
      }
    }
  },
  "word_filter": {
    "description": "输出过滤器配置",
    "type": "object",
    "hint": "将机器人输出消息中的过滤词替换为固定词",
    "items": {
      "enabled": {
        "description": "是否启用输出过滤器",
        "type": "bool",
        "default": true,
        "hint": "启用后，机器人输出消息中命中的过滤词会被替换为固定词"
      },
      "filter_words": {
        "description": "过滤词库（多个词用逗号隔开）",
        "type": "string",
        "default": "",
        "hint": "需要被替换的词，多个词之间用逗号隔开，例如：词1,词2,词3。修改保存后立即生效"
      },
      "replacement": {
        "description": "替换词",
        "type": "string",
        "default": "filtered",
        "hint": "命中的过滤词将被替换成这个词"
      }
    }
  }
}
